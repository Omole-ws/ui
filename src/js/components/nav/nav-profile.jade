.ui.simple.dropdown.item(class=cls)
    i.ui.orange.user.icon
    =username
    i.dropdown.icon
    if isFetching
        .ui.active.dimmer
            .ui.small.loader
    .ui.vertical.menu(class=isFetching?'disabled':'')
        .link.item(onClick=logout)
            i.sign.out.icon.right
            | Logout
